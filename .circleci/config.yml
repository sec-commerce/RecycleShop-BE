version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  deploy:
    docker:
      - image: circleci/node:latest

    working_directory: ~/repo

    steps:
      - checkout

      - add_ssh_keys:
          fingerprints:
            - "e2:6e:4a:e4:87:6d:dc:3a:76:82:ef:3a:5b:c5:fb:6d"

      - run:
          name: deploy
          working_directory: /
          command: |
            ssh kvisnia@cloud.kvisnia.me 'bash /home/kvisnia/vendure/build.sh'

workflows:
  version: 2
  build:
    jobs:
      - deploy:
          filters:
            branches:
              only:
                - master
